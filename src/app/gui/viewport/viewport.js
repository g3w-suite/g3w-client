import ViewportService from 'services/viewport';
import ViewportComponent from 'components/Viewport.vue';

/**
 * TODO: split and refactor into dedicated file (eg. components/ViewportComponent.vue)
 */

module.exports = {
  ViewportService,
  ViewportComponent
};
